package translations

const path = "../../fixtures/translations.txt"

// Translate replaces the value(s) of a `translation key(s)` in a string `s`
// against a predefined lookup
func Translate(s string) string {
	return s
}
